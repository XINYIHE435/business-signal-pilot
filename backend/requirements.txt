fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.7.4
pydantic-settings==2.1.0
anthropic==0.39.0
openai==1.12.0
duckdb==0.10.0
polars==0.20.0
structlog==24.1.0
python-multipart==0.0.6
pytest==7.4.4
pytest-asyncio==0.23.3
httpx>=0.27.0,<1.0.0

# === Phase 1: LangGraph Multi-Agent ===
langgraph>=0.2.34,<0.3.0
langchain-core>=0.3.17,<0.4.0
langchain-anthropic>=0.3.0,<0.4.0

# === Phase 3: RAG System ===
chromadb==0.5.18
sentence-transformers==3.2.1

# === Phase 4: Export Layer ===
markdown==3.7
# PDF 渲染引擎（按优先级回退）：
#   weasyprint 排版最佳，但依赖系统级 GTK/Pango/Cairo（Linux/Docker 友好，Windows 需额外安装 GTK 运行时）
#   fpdf2 为纯 Python 后备引擎，无系统依赖，保证 Windows 等环境也能导出中文 PDF
weasyprint==62.3
fpdf2==2.8.7

# === Phase 4: Observability (Optional) ===
# langsmith==0.2.0
